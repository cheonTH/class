git
- 개념
- 명령어
- github와의 차이

HTML
- 문서의 구성
- 속성의 종류와 쓰임
- 태그

CSS
- 스타일의 종류
- css 선택자(태그, 클래스 ,아이디, 속성)
- 적용 우선순위(인라인, style태그, 외부스타일)

Java Script
- 자료형 (데이터 타입은 무엇이 있는가?)
- 변수 (생성, 대입, var, let, const의 차이)
- 연산자 (연산자의 종류, 우선순위)
- 조건문 (if, switch, for, while, break, continue)
- 배열 (배열의 생성, index, 메서드)
- 객체 (객체의 생성, 프로퍼티)
- 함수 (함수의 개념, 생성, 호출, 메서드와의 차이)
- 이벤트 (이벤트의 개념, 이벤트의 종류와 사용법, 이벤트 처리)
- 데이터의 전송(form, input, http method)
- 비동기통신(ajax)